# API URL for the backend
VITE_API_BASE_URL=https://your-backend-url.com/api/v1

# Socket.io URL (if different from API base, usually same host but different port/path)
VITE_SOCKET_URL=https://your-backend-url.com
